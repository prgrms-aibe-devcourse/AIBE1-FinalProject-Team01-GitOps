apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-slave-storage
  namespace: amateurs
spec:
  storageClassName: bulk-hdd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
